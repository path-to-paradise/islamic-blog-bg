---
const SITE_NAME = "Светлина и знание";
const SITE_TAGLINE = "Научете за Рукя, Исляма и невидимия свят";

const base = import.meta.env.BASE_URL.replace(/\/$/, "");
const homeHref = `${base}/`;
const searchHref = `${base}/search`;
const downloadsHref = `${base}/downloads`;
const aboutHref = `${base}/about`;
const contactHref = `${base}/contact`;
---

<header class="site-header">
  <div class="wrap">
    <div class="header-top">
      <a href={homeHref} class="brand">
        <svg class="brand-mark" viewBox="0 0 40 40" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
          <path d="M20 3 C27 12 29.5 18.5 25 24.5 C23.3 26.8 16.7 26.8 15 24.5 C10.5 18.5 13 12 20 3 Z"
            fill="#e0b84b" opacity="0.95"/>
          <ellipse cx="20" cy="30.5" rx="12" ry="3.4" fill="#e0b84b" opacity="0.95"/>
          <rect x="8" y="33.5" width="24" height="3" rx="1.5" fill="#e0b84b" opacity="0.75"/>
        </svg>
        {SITE_NAME}
      </a>

      <button
        type="button"
        class="nav-toggle"
        id="nav-toggle"
        aria-label="Превключване на менюто"
        aria-expanded="false"
        aria-controls="site-nav"
      >
        <svg viewBox="0 0 24 24" width="22" height="22" aria-hidden="true">
          <rect y="4" width="24" height="2.4" rx="1.2" fill="currentColor" />
          <rect y="10.8" width="24" height="2.4" rx="1.2" fill="currentColor" />
          <rect y="17.6" width="24" height="2.4" rx="1.2" fill="currentColor" />
        </svg>
      </button>
    </div>

    <p class="site-tagline">{SITE_TAGLINE}</p>

    <nav class="site-nav" id="site-nav">
      <div class="nav-links">
        <a href={homeHref}>Начало</a>
        <a href={downloadsHref}>Материали</a>
        <a href={aboutHref}>За нас</a>
        <a href={contactHref}>Контакти</a>
      </div>

      <form class="header-search" action={searchHref} method="get" role="search">
        <input type="search" name="q" placeholder="Търсене в публикациите…" aria-label="Търсене в публикациите" />
        <button type="submit" class="header-search-btn" aria-label="Търсене">
          <svg class="search-icon" viewBox="0 0 20 20" aria-hidden="true">
            <circle cx="9" cy="9" r="6.5" fill="none" stroke="currentColor" stroke-width="1.6" />
            <line x1="13.6" y1="13.6" x2="18" y2="18" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" />
          </svg>
        </button>
      </form>
    </nav>
  </div>
</header>

<script>
  const toggle = document.getElementById("nav-toggle");
  const nav = document.getElementById("site-nav");

  if (toggle && nav) {
    toggle.addEventListener("click", () => {
      const isOpen = nav.classList.toggle("is-open");
      toggle.setAttribute("aria-expanded", String(isOpen));
    });

    nav.querySelectorAll("a").forEach((link) => {
      link.addEventListener("click", () => {
        nav.classList.remove("is-open");
        toggle.setAttribute("aria-expanded", "false");
      });
    });
  }
</script>
